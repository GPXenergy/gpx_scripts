#!/bin/bash

python /app/main.py --identifier $IDENTIFIER --api $API --api_key $API_KEY --participation_total $PARTICIPATION_TOTAL --participation_client $PARTICIPATION_CLIENT --start $START --gpx_key $GPX_KEY --gpx_meter_id $GPX_METER_ID
